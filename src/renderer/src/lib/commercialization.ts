/**
 * Commercialization remains a product and entitlement draft during Beta.
 *
 * Keep the existing Pro UI and capability boundary available for iteration,
 * but do not expose it until activation is backed by a real signed entitlement
 * and the free/paid split has been explicitly approved.
 */
export const COMMERCIALIZATION_DRAFT_ENABLED = false
