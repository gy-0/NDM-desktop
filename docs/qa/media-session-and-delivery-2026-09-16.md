# 浏览器会话与媒体成品验收

本次修复随Motrix功能升级继续推进。2026091601已签名安装并核对原任务保留；真实Chrome页面下载和安装版资料读取问题仍在处理，不将自动测试或成功安装视为全部验收完成。

## YouTube进度与成品

用户链接：`https://www.youtube.com/watch?v=ReogxIL1rBk`，页面标题“Five questions about AI—and my answers”，时长约162秒。用户报告1080p高码率估算200多MB，完成时突然变为40多MB；原用户文件尚未定位，不能将新下载直接视为原文件鉴定。

- 使用当前Chrome Default登录态、固定包内yt-dlp，精确选择`616+140-20`完成高码率下载：44,041,630字节，VP9 1920×1080、24000/1001fps、3879帧、视频161.786秒，AAC音频161.843秒。全文件ffmpeg解码退出0且无错误。
- 高码率成品SHA-256：`9b3f1605a575246f99785906f9d2b08490d65401fea4d5cecf3e291b45611f14`。普通1080p `137+140-20`对照为41,043,055字节，H.264，同帧数和时长。40多MB本身不能证明降档或缺音轨。
- 真实HLS进度估算一度达到91,800,149字节，视频实际接收41,389,060字节。将同一进度序列送入生产实现，旧逻辑以`max`锁住早期大估算、到下载结束仍显示约94.42MB；修复后合流前已收敛至44.009MB接收量，成品44.041MB。本次未复现原报告200MB峰值。
- `0c99ab0`（集成后`4b1135d`）修复macOS/Windows动态估算和结束时虚增已下载字节，保留尚未下载音轨的估算。原生34项定向测试通过；TS640项通过、9项显式集成跳过；类型检查、renderer和Release Host构建通过。
- 同时修复默认跳过失败分片仍报告成功的问题：本机缺片夹具应有40帧，旧参数退出0却仅30帧；启用`--abort-on-unavailable-fragments`后退出失败且没有发布最终文件。高码率实测也启用此参数。
- 汇总回执：`/tmp/ndm-youtube-qa-20260916/verification.json`。含签名URL的原始探测JSON已清除，没有导出全浏览器Cookie文件；测试下载和夹具进程均结束。

## 浏览器会话与抖音

用户链接：`https://www.douyin.com/jingxuan?modal_id=7684024843209051426`。

- 当前Chrome Default确实为活跃资料，本地资料名“用户1”，`last_used`与`last_active_profiles`均指向Default，Cookie数据库存在；不能断言本次由选错Profile引起。原实现仅保存品牌，裸`--cookies-from-browser chrome`由yt-dlp按Cookie数据库时间选资料，且设置隐藏了下拉箭头。
- `25fed02`（集成后`23ae707`）按浏览器明确活跃资料解析来源，显示受限的本地资料名与可见箭头；解析、失败重试和创建保留精确来源。多活跃资料或不可用资料要求明确选择，避免静默切Default；已有Relay来源优先。655项TS测试中646通过、9跳过，类型检查/构建和7项原生来源测试通过。
- 真实Chrome页面可播放该视频，标题“12个苹果的 iPhone Duo 折学”，作者大耳朵TV，时长06:59；Relay显示6个捕获版本，其中推荐1440p约74MB。这证明页面可访问，尚不能单凭入口显示声称已下载成功。
- 真实点击该1440p版本后，已安装1502弹出只含CDN地址的添加窗口，预检未识别文件，尚未提交。代码核对发现Host将无扩展名的已捕获媒体地址误判为网页，转交Composer时只保留URL/标题及可选页面会话，丢失原资源的请求头与来源页。该已存在入口已由下述`ef47d15`修复，仍待安装版实测；用户原失败任务3800保持原样。
- 显式Chrome Default能读取该站点会话；独立原生详情API仍返回HTTP403。旧Douyin工作稿将403/429和空响应误报为缺少登录态。`3bb4588`（集成后`8f1e225`）保留站点拒绝分类`siteRequestRejected`，Cookie按实际请求域、路径、HTTPS和有效期筛选，取消跨Profile回退；导出通过私有管道处理，避免全浏览器Cookie临时落盘。
- 手动粘贴页面URL原先没有Relay候选查询RPC。新版已补齐明确查询当前网页、选择捕获版本、保留来源和创建回执的流程（见下文），尚待最终安装版的真实Chrome验收；未将独立API403记为解决，未用另一视频代替用户指定视频验收。

## 文件名与浏览器交接补充

- `6c889ae`新增可空的`requestedFilename`，将用户确认的文件名与真实来源标题分开保存。普通创建及yt-dlp创建都保留显式名称，服务端URL建议名称不冒充用户选择；完成时使用已确认的名称主体，扩展名以引擎实际产出容器为准。同名现有文件仍保留。旧库迁移、插入/更新/重开、创建回执及真实HTTP完成等28项定向测试通过；没有修改生产库。
- `53989b4`（集成后`5f7f35d`）加入明确读取当前抖音页面和选择捕获版本的两阶段接口：列表只含摘要及临时选项标识，创建时才从原连接获取请求。支持原始`jingxuan?modal_id=`和标准`/video/`，绑定来源连接、页面、文档和视频版本，页面变化后要求重读；相同标题的多个不可区分来源转到原浏览器选择。未决创建回执与新表单隔离。
- 同一提交修复扩展未发送已有MIME/大小，以及版本说明覆盖User-Agent的问题。新单资源接口明确拒绝双轨候选，避免把第二音轨URL误当成文件名后无声完成。扩展243项、原生29项、TS654通过/9跳过及类型检查/构建通过；以上是模块提交证据，最终集成仍另行检查。
- `32a56bf`（集成后`ef47d15`）修复旧扩展入口：明确捕获的媒体保留完整请求交给引擎，`media-page`及无媒体证据的网页继续解析。兼容旧字段3第二音轨，并保留现有HLS独立音轨路径；冲突音轨不静默退成单轨。25项定向测试通过；真实隔离Host经WebSocket收到合成媒体后下载196,608字节且逐字一致，鉴权/来源/真实User-Agent保留，只创建1任务且不弹Composer；两个网页负例零任务零请求。私有支持目录、Host、HTTP服务、端口和隔离偏好均已清理。

## 安装与边界

2026091601已安装到`/Applications/NDM.app`，真实设置页显示2026.9.16/build2026091601、Native Swift engine。安装前确认3729条任务中没有正在下载或等待的任务，经应用菜单正常退出后保留旧2026091502应用，再替换签名包。重启后的下载3729条、请求头708条、Relay回执33条及其他原表逐字段摘要与安装前一致；新可空字段迁移没有改变旧任务内容。私有回执位于`/tmp/ndm-install-verification-20260916/`，原应用和数据库备份保留。

已安装ASAR SHA-256：`5181d9a998e684753ea4d2481cde1841d019e70876f9d5411485ca3d9bcc1c8d`；签名Host SHA-256：`9d0d05778148010eb1e0d216a77b6c93bda2192626fd04c69aea639261f78d57`。稳定Apple Development签名及deep/strict验证通过；固定工具摘要、对应源码及许可保持有效。

真实安装版设置仍显示“未找到Chrome的可用个人资料”，点击重新检测后相同。当前Chrome元数据明确指向Default（用户1），等价受限读取和同Electron的Node-mode服务均可正确识别3份资料，尚未证明应用进程具体读取失败原因；不能归因于用户选错Profile或擅自声称系统权限拒绝。正在补齐实际失败原因和稳定读取诊断，后续安装验收单独记录。

现有解压扩展源码已更新至1.4.16，Chrome中运行的旧扩展尚待用户手动重新加载。浏览器工具明确禁止打开扩展管理页及换通道绕过，已请求用户完成此一步。真实原抖音页面的新版交接和最终可播放文件尚待验证；独立详情API的HTTP403未记为修复。Windows后端代码检查与macOS夹具不能替代Windows操作系统验收。

中间集成`8f1e225`：TS 657项中649通过、8跳过，类型检查和构建通过。原生全量Core554、Bridge24及Swift Testing 11项通过；Engine662项中28跳过，配置隔离的一项测试在新复制工具首次启动时两次达到15秒超时，产生4个断言失败。同源码、同工具原样定向重跑2/2通过（实际工具用例2.736秒），未删扩展属性或改动测试。保留首次失败与重跑日志，不将该次全量写成首次通过。后续Relay与旧入口路由修复仍需重新验证并编译Host。

最终源码集成`ef47d15`（版本2026.9.16/build2026091601）：TS 663项中655通过、8跳过，类型检查和构建通过，Relay243/243通过。新原生全量首跑退出0，无需重跑：Engine673项中28跳过、其余通过，Core555、Bridge32及Swift Testing 11项全部通过。私有汇总`/tmp/ndm-media-integration-20260916/.build/1601-validation.json`保留完整日志路径。

新Release Host及实际签名包内Host分别通过两条严格交接脚本：页面查询预览零传输、仅选定来源读取、524288字节逐字一致，文件名/请求头/来源页保留，并发与断线回执重试仍只有一个任务；旧扩展捕获入口196608字节一致、保留来源与真实User-Agent、不弹Composer，两个网页负例只进入解析而不下载。测试专用Host、服务、端口及偏好均清理。包内Electron的Node-mode也实际加载hash-wasm并通过MD4向量。该包级证据不替代前述待完成的真实Chrome验收。

## 1602资料读取诊断候选包

`24133e7`加入有限长度的循环读取、最多三次不稳定快照重试，以及明确的读取阶段和安全错误原因分类。非普通文件及超过8MiB的文件不读取；无可用来源时仍不猜测其他资料。只检查Cookie数据库是否存在，不打开数据库。当前选定资料发生访问错误会阻断，其他未选定资料的访问错误不会误报为当前资料失效。错误详情在设置页默认折叠，主要提示保持可操作的说明；不输出路径、Cookie、原始系统错误消息或账号内容。

原实现和新实现均已在无窗口、普通运行模式的Electron43.4.0中成功读取当前Chrome资料，系统用户目录比较也一致；安装版Electron的Node-mode同样成功。这些结果无法证明真实GUI失败是短读、权限拒绝或选错资料，下一步仍需真实安装版展开诊断定位。

该补丁TS682项中673通过、9项显式集成跳过，类型检查和构建通过。`b688d2f`准备2026091602版本；标准打包完成Native缓存构建及renderer构建后卡在Electron网络下载阶段，停止本次自有builder，使用已校验的离线Electron43.4.0完成相同builder及签名流程。最终deep/strict、ASAR源码镜像、Relay镜像、固定工具及源码许可、包内MD4检查通过；原生输入Host与1601一致，没有借此声称重新执行了全量原生测试。

1602 ASAR SHA-256：`5c4c0108bf282b5e931e277e5df73143a63070714f6173a29f507b914e5a61ae`；签名Host SHA-256：`eb2cec9eee672b55e332ecf18b1db4b8f619de8ef03003bc3f64b1d0051780f7`。回执`/tmp/ndm-media-integration-20260916/.build/1602-release-receipt.json`。安装暂存副本的签名及这两个摘要再次一致。

准备正常退出1601前，界面工具报告Mac重新锁屏且不能自动解锁，已请求用户手动解锁；未退出或替换正式应用。正式运行版本仍为1601，1602仅完成安装暂存，部署记录`/tmp/ndm-install-verification-2026091602/deployment.json`中的`swapped`保持false。解锁后需要重新核对无活动任务、捕获最新保留基线、正常退出与替换，再验收实际设置页。Chrome扩展手动重新加载和原抖音视频下载也仍待完成。
