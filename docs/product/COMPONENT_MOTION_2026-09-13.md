# Component motion · 2026-09-13

首批打磨覆盖任务动作、数量、添加下载弹窗和批量操作条。参考 Sanyam Lab 的交互方向，使用 NDM 已有的 Motion、Base UI 和 Lucide 独立实现；没有复制其组件源码或新增依赖。

- `TransferActionIcon` 在继续、暂停、下载、等待回执之间过渡。按钮尺寸、标签、禁用条件和失败反馈仍由原业务控制。录制保留“停止并保存”。
- `AnimatedCount` 用两个固定视觉层呈现数字切换，读屏只读取当前值。快速连续更新取消旧动画；CSS 时间值同时处理 `ms` 和压缩后的 `s`。
- Composer 的根组件保持挂载，由 Base UI 管理进入和离开；正文自然高度变化用 `ResizeObserver` 测量，底部操作栏保留在可滚动正文之外。
- 批量条在原网格区域内淡入淡出，任务列表不位移。退出的控件立即变为 inert，焦点回到搜索框。
- 共用一个动态偏好监听器；运行中开启“减少动态”会结束位移、旋转及高度动画。

## 验证

执行 `npm test`（435/435）、`npm run typecheck`、`npm run build` 和 `node scripts/qa-component-motion.mjs`。

生产构建运行于真实 Electron main/preload/renderer，使用私有 TCP 引擎夹具、合成任务和独立的用户目录、支持目录及端口。六组交互验证通过：

1. 等待回执期间显示忙碌；失败保留原任务状态，重试成功后切换动作。
2. 12 → 9 → 10 → 11 → 9 的连续数量变化捕获到 38 个中间帧，最终值及读屏值准确。
3. 雾昼主题、740 × 680 窗口：弹窗入场、选项高度变化、焦点约束和快速开关正常。
4. 深色主题下同样通过；两种主题的正文高度均从 138 px 过渡到 234 px，底部按钮完全位于窗口内，横向溢出为零。
5. 运行中切换减少动态：数字、等待图标及弹窗无位移或旋转。
6. 2,000 条合成任务仅挂载 16 行，末行可达，数量正确且无横向溢出。

退出后夹具两个端口均关闭、连接归零、renderer 错误为零。脚本在开始和结束比较构建指纹，避免把中途重建的结果混入证据。

这是 Electron 界面验收，不包含真实网络下载或已安装 macOS 应用的发布验收。本批未改下载引擎，也未替换 `/Applications/NDM.app`。

## 第二批：工作区连续反馈

- 快速操作：一块高亮连续跟随键盘和鼠标，搜索结果区自然伸缩；滚动与高亮测量使用局部坐标。文本不缩放，中文输入法与执行去重保持原逻辑。
- 任务详情：下载设置、错误详情、完成文件使用原生 `details/summary` 的内在高度过渡，可随时反向操作；收起立即使内容 inert，减少动态直接到达终态。
- 完成/安装通知：同一文件保持同一通知，状态图标和文案平滑交接。错误详情在操作区下方展开，重试回执不会使按钮移位。
- 剪贴板提示：与通知统一层次和操作尺寸，适配窄区域；修复生产 CSS 秒单位导致的提前卸载，补齐重复触发保护、退出焦点与关闭名称。
- 右键菜单：Root 持续挂载，Base UI 完整管理进入/离开，关闭回到对应任务。移除菜单项的按压文字缩放。
- 链接拖入：只在接收区域的图标内显示流动虚线；热点不再缩放整张卡片，命中区域和文字保持稳定。原生文件仍被排除，放下链接仍先进入下载确认。

实现使用项目现有依赖和中性主题，没有引入参考站点源码或新包。

第二批沿用同一个生产 Electron QA 入口，增加两个场景模块；共 12 组通过，包含第一批回归。新增证据包括：详情高度 39 → 219.8 px、快速操作结果区 396 → 139 px 的中间帧，高亮最终位置/尺寸偏差均为 0；中途反转、减少动态、IME 不误执行、菜单退出焦点、剪贴板离场、安装合成失败回执均通过。保留 15 张渲染截图与逐帧数据，人工抽查了快速操作、通知和链接接收区域；renderer 错误为零，夹具端口与连接全部关闭。

`npm test` 435/435、类型检查和生产构建通过。原有拖放测试引用的内联源码位置与缩放效果已随组件提取更新，接收区域尺寸稳定另由真实 Electron 几何检查覆盖。此批依旧未执行真实安装或替换已安装的 NDM。

## 第三批：可直接操作的文件组件

从细节反馈扩展到主界面结构，参考 Document Pocket / Folder Stack、Tab Overview / LabPreview、Island Menu / Split to Edit 的交互方向，用现有依赖独立实现。

- 卡片视图作为首次使用的默认视图；工具栏可切回列表并记住选择。大幅文件预览、文件类型纸卡、下载进度和文件操作进入主界面，复用真实任务与文件交付路径。
- 最近完成文件口袋以堆叠文件卡呈现最近五项，展开后可逐项查看和操作；筛选范围与当前文件库保持一致，搜索时让出空间。
- 传输胶囊展开为进度面板，显示总速度、活动/等待数量和实际任务；临时限速在当前读数与编辑控件之间展开。
- 合成视觉样例使用 `tasks=showcase&layout=cards`，与 `tasks=sample` 和 `tasks=empty` 分开；示例插画只由浏览器夹具返回，不注入产品中的真实文件数据。

第三批验证：`npm test` 435/435、类型检查和生产构建通过。生产 Electron 共 19 组场景通过、21 张截图、renderer 错误为零。新增场景验证 2,000 个任务最多装载 48 张卡片并可到达末页；暂停等待引擎回执；打开/预览/访达使用正确路径到达 Electron IPC；文件口袋展开的中间帧与操作回执；传输岛的入场、限速编辑回焦点；搜索范围外的任务可从岛内打开详情；小窗口和减少动态下完整露出首个文件操作；DMG 安装防重复与失败重试。

文件口袋与卡片共享当前选择。列表存在大传输卡时不再叠加文件口袋，保证列表可用空间。传输岛不会因当前搜索或筛选排除目标而产生无效点击。

测试仍使用独立用户目录、支持目录和端口；文件/安装回执由测试拦截，不执行真实安装，夹具结束后端口与连接归零。已安装的 `/Applications/NDM.app` 尚未替换。
