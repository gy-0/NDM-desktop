# NDM 2026.9.8

本机构建：2026090820。未作为公开商店版本发布。

## 构建 2026090820

- 主下载区域：长文件名不再撑宽内部布局并挤走速度、暂停按钮。
- 窄区域将文件信息和速度/操作排成上下两行，保留真实进度背景及任务切换。

288 项界面/脚本检查、类型检查、构建通过。真实 Electron 六组窗口/zoom（100%、125%）全程保持详情打开，标题、速度单位、按钮无越界或重叠；最窄暂停态以及模拟暂停/继续操作通过。已查看完整原生窗口截图。

正式签名包的同组六项真实 Electron zoom 验收再次通过，含暂停态和控制调用；日志 `/tmp/ndm-hero-zoom-packaged.log`。

已安装启动 `/Applications/NDM.app` 构建 2026090820，安装后的 app.asar 与 NDMHost SHA-256 匹配最终签名包；新 App/引擎健康后永久清理旧部署包。日志 `/tmp/ndm-hero-zoom-deploy.log`。

## 构建 2026090819

- 窄列表：下载中任务用第二行补回被收起的速度和剩余时间；宽列表不重复显示，暂停后恢复类型/来源。
- 详情：增加预计剩余时间，无法估算时显示计算中。
- 极窄列表：百分比和进度条移到任务行底边，图标让位；合集同步适配，维持虚拟列表行高。

288 项界面/脚本检查、类型检查与构建通过。正式包渲染器的 12 组宽度/详情组合、长 ETA、未知速度、暂停清除及合集展开收起验证通过；已查看常规和极窄截图。125% 使用可用 CSS 宽度压力模拟，不代表实际 Electron zoom 验收。日志 `/tmp/ndm-compact-metrics-packaged.log`。

已安装启动 `/Applications/NDM.app` 构建 2026090819，安装后 app.asar 与 NDMHost SHA-256 匹配正式包。新 App/引擎健康后永久清理旧部署包；正式包临时解包目录也已清理。部署日志 `/tmp/ndm-compact-metrics-deploy.log`。

## 构建 2026090818

- Relay 1.4.8：连接已经关闭或发送抛错时保留请求并重连，防止点击被静默丢弃。
- 旧连接的迟到事件不再污染新连接；旧桥接 HEAD 补充信息失败或超时不再抹掉下载意图。

108 项 Relay 检查与 20 项隔离 Chrome fixture 通过，包含新旧代码失败/通过对照。本批未修改原生引擎或 React UI；打包构建通过。逐请求原生 ACK 和 worker 退出后的持久队列仍未实现，不据此承诺绝对不丢任务。

正式包的真实 WebSocket→独立 Host 下载验收通过：断线重连后仅创建一个任务，65,536 字节文件与本地服务器原始 SHA 一致。Chrome API 为 stub；此项不是用户 Chrome 已更新的证明。日志 `/tmp/ndm-relay-recovery-packaged-handoff.log`。

已安装启动 `/Applications/NDM.app` 构建 2026090818，包内 Relay 1.4.8；安装后 app.asar、NDMHost、worker 与正式包哈希一致。健康后永久清理本次旧部署包，未更改用户 Chrome 的扩展安装。日志 `/tmp/ndm-relay-recovery-deploy.log`。

## 构建 2026090817

- macOS 视频工具固定更新至官方 yt-dlp 2026.08.19，移除过时的 YouTube 客户端覆盖，使用该固定版本维护的默认策略。
- 媒体解析和下载忽略外部命令行配置，避免用户另一份 yt-dlp 配置改变 NDM 的输出目录、cookies 或格式；NDM 显式设置继续保留。Windows 同步配置隔离，但工具升级和真实运行验收仅覆盖 macOS。

旧工具加新客户端策略能列出 1080p，却两次实际下载失败；因此没有只凭解析成功交付。固定新工具与新 Host 已在隔离环境完成同一公开 Blender 视频的 1080p 下载，实际检查到 AV1 1920×1080＋AAC。MP4 表示容器，不承诺 H.264 或所有播放器兼容。本次候选 Bilibili 公开样本可列出四档；Vimeo 匿名访问仍需浏览器会话，不宣称全站支持。

288 项界面/脚本检查、类型检查、构建，以及 939 项 XCTest（7 项环境跳过）和 11 项 Swift Testing 通过。另有真实工具配置文件对照，验证外部配置确实被隔离、显式参数仍生效。完整原生回归运行于升级前工具；升级后真实工具配置及媒体格式 33 项回归再次通过；正式签名包内八组预览样本、四类访问限制和八个非法会话请求验收通过。

正式签名包内真实 1080p 下载再次通过：40.983 秒完成 82,056,145 B，音视频格式及输出 SHA 与候选验证一致，隔离媒体和缓存已清理。日志 `/tmp/ndm-media-packaged-download.log`。

已安装启动 `/Applications/NDM.app` 构建 2026090817，安装后 app.asar、NDMHost 与 yt-dlp SHA-256 均匹配最终签名包。新 App 与引擎健康后永久清理本次旧部署包。部署日志 `/tmp/ndm-media-toolchain-deploy.log`。

## 构建 2026090816

- 添加下载：使用现有 BaseUI 对话框机制管理键盘焦点，保留主区域底部位置；关闭后返回打开它的控件。链接、重命名输入及选项展开状态有明确的辅助技术语义。
- 分段选项控件：高亮使用自身容器内坐标，避免展开内容、滚动及调整窗口时发生错误的纵向位移动画；保留选择切换反馈。

已安装旧版本的真实键盘测试复现 Tab 逃到背景侧栏、没有对话框名称及关闭后失去原焦点。逐帧视觉测试另复现格式高亮在滚动/布局变化时最大约 74 像素的瞬时纵向偏移，静止后检查会漏掉此问题。此次测试使用隔离 Electron 和模拟媒体结果，不操作用户任务。

288 项界面/脚本检查、类型检查及构建通过。真实 Electron 的 50 次正反 Tab（每次等待两帧使库的焦点守卫稳定）、非输入控件 Escape、焦点返回和输入名称通过；920×600 窗口下展开选项无横向溢出，预览及浏览器会话回归通过。逐帧高亮位移为 0，横向切换有中间帧，动态启用减少动效后立即停用滑动。未执行屏幕阅读器实际朗读验收，不据此宣称所有辅助技术体验已完善。本批未修改原生引擎源码。

正式签名包内键盘和逐帧动效验收通过，并查看实际截图。已安装启动 `/Applications/NDM.app` 构建 2026090816，安装后的 app.asar 与 NDMHost SHA-256 均匹配最终签名包；健康后永久清理旧部署包。日志 `/tmp/ndm-composer-packaged-keyboard.log`、`/tmp/ndm-composer-packaged-motion.log`、`/tmp/ndm-composer-polish-deploy.log`。

## 构建 2026090815

- 视频预览：macOS 解析明确返回番剧预览信号时，显示“当前仅提供预览”，仍可选择清晰度和下载。
- 准确性：仅识别已核实的提取器及警告，不按短时长、会员画质或字幕提示猜测；换链接及重新解析清除旧提示。
- 信息边界：成功结果只传预览枚举，不向界面暴露原始工具诊断。其他站点、Windows 解析器及实际账号下载兼容未在本批扩展。

288 项界面/脚本检查、类型检查及构建通过。真实隔离 Electron 验证普通/会话两条成功路径、普通短视频不误标及预览可下载。真实独立 Host 的八组工具样本通过，包含分块警告、无末尾换行、五类负例和缓存复开；旧 installed14 的三个正例缺提示已复现。首次原生样本测试遗漏缓存隔离，可能触发现有过期缓存清理，影响未确认；最终测试改为每次独立临时缓存，此问题已告知用户并记录。

完整原生回归通过：937 项 XCTest（7 项环境跳过）、11 项 Swift Testing 零失败。上一批真实 Electron 的浏览器选择回归、Host 的非法会话及访问分类回归均通过。日志 `/tmp/ndm-preview-full-native.log`、`/tmp/ndm-preview-session-regression.log`、`/tmp/ndm-preview-access-regression.log`。

正式签名包内 Host 八组样本与 Electron 预览交互验收通过，已查看实际截图。已安装启动 `/Applications/NDM.app` 构建 2026090815；安装后的 app.asar 和 NDMHost SHA-256 均匹配最终签名包，健康后永久清理旧部署包。日志 `/tmp/ndm-preview-packaged-host.log`、`/tmp/ndm-preview-packaged-ui.log`、`/tmp/ndm-preview-deploy.log`。

## 构建 2026090814

- 视频会话：解析失败后可选择实际登录的浏览器，选择本身不读取会话。明确重试后，解析、后续重试与新建下载保留所选来源；可在失败后改选，且不改全局偏好。
- 请求保护：原生入口拒绝非法的显式浏览器值，不再默默匿名处理；已保存媒体配置损坏时明确停止续传，保留任务配置供恢复。
- 兼容：普通匿名重试保持匿名；无保存选项的旧任务保留默认行为，有效浏览器及 cookie 文件配置正常恢复。

287 项界面/脚本测试、类型检查及构建通过；完整原生 933 项 XCTest（7 项环境跳过）与 11 项 Swift Testing 零失败。真实隔离 Electron 验证 Firefox 重试及切换 Safari 后的下载参数、无自动会话读取和全局偏好不变。真实独立 Host 拒绝八个非法会话请求，四类访问错误回归通过；旧 installed13 的静默匿名行为已由同一脚本复现。此处使用模拟 IPC 与临时工具样本，不代表实际账号或网站兼容已验证。

正式签名包内上述 Host 与两组界面验收均通过，并检查了实际界面截图。已安装启动 `/Applications/NDM.app` 构建 2026090814，安装后的 app.asar 与 NDMHost SHA-256 均匹配最终签名包；健康检查后永久清理本次旧部署包。日志 `/tmp/ndm-session-packaged-ui.log`、`/tmp/ndm-session-packaged-host.log`、`/tmp/ndm-session-deploy.log`。

## 构建 2026090813

- 视频访问提示：地区限制、会员权限与登录/会话读取失败分别处理；地区限制不再引导反复登录，会员权限只在用户明确选择时尝试会话。
- 错误原因：多行工具输出中的通用 cookies 提示不再覆盖更具体的地区/会员原因。
- 下载保护：明确访问限制时，不把未知网站的视频页面退回作为 HTML 文件下载；保留输入和重试操作。

真实隔离 Host RPC 与工具错误样本验证四种类别，旧安装版将地区/会员误归登录，新 Host 正确且未创建任务。真实 Electron 验证按钮、显式会话选择及阻止 HTML 误下载。285 项界面/脚本测试、类型检查与构建通过；完整原生回归通过：927 项 XCTest（7 项环境跳过）与 11 项 Swift Testing 零失败。正式签名包内 Host 与界面验收通过。已安装启动 `/Applications/NDM.app` 构建 2026090813，安装后的 app.asar 与 NDMHost 均匹配最终包 SHA-256；健康检查后已永久清理旧包。测试未访问真实受限网站、读取 cookies 或证明绕过访问条件。

## 构建 2026090812

- 视频解析：会话数据暂不可用、普通解析异常均可原地重试；保留链接、自定义文件名和显式选择的 Chrome 会话，不自动创建任务。
- Relay 1.4.7：新连接探测取消旧重试，防止“已连接”被迟到结果改回“未连接”；备用端口重试不受影响。
- 扩展安装：设置内可复制本地扩展路径，沿用已有动画反馈，复制失败可重试。

282 项界面/脚本测试、类型检查、构建、102 项 Relay 检查与 17 项隔离 Chrome fixture 通过。真实 Electron 验证会话重试和路径复制。正式签名包内两项交互验收通过。已安装启动 `/Applications/NDM.app` 构建 2026090812，安装后的 app.asar 与 NDMHost 均匹配最终包 SHA-256；健康检查后已永久清理旧部署包。随包 Relay 为 1.4.7，未宣称用户 Chrome 已重载新版。

## 构建 2026090811

- 下载存储：新的已知长度、具有强文件标识且支持 Range 的 HTTP 任务改为单文件分段写入，完成时直接发布，不再复制整份文件来合并。已有旧任务和不满足条件的单流路径继续旧存储。
- 恢复与清理：暂停保存持久化进度，强制退出后恢复；删除和重新下载只清理确认归属的临时文件。已发布文件恢复无需重新访问源站。
- 自动命名：改名保留可恢复记录，校验文件身份与长度；保留旧任务无缓存目录时的正常删除行为。

真实 debug Host 的 64 MiB、32 连接下载通过暂停/恢复、SIGKILL/新进程恢复与独立 SHA-256 验证；采样任务文件峰值约 64 MiB＋28 KiB，无分段合并副本。完整原生回归通过：920 项 XCTest（7 项环境跳过）与 11 项 Swift Testing 零失败。正式签名包内 Host 的同一验收和真实 Electron 界面回归均通过。已安装启动 `/Applications/NDM.app` 构建 2026090811，安装后 app.asar 与 NDMHost 的 SHA-256 均匹配最终包，健康检查后已永久清理本次旧包。

## 构建 2026090810

- 媒体交接：迟到解析不再覆盖新链接、重新打开已关闭弹窗或关闭设置；手动输入与当前任务选择不被旧结果抢走。独立文件请求仍正常创建。
- 存储开发：新增原生单文件存储后端、归属校验与持久化进度记录。尚未接入正式下载/清理/空间预算，用户任务仍使用旧分段文件与合并流程，不能宣称已消除两倍空间占用。

真实 Electron 的乱序、关闭、设置、手动输入及迟到 binary 请求验收通过。282 项界面/脚本测试、类型检查、构建通过。14 项新存储针对性检查通过，真实 XCTest 子进程 SIGKILL 后由另一进程恢复并发布，4 MiB 文件经 Node 独立校验哈希一致，最终分配空间 4 MiB＋8 KiB。完整原生回归通过：878 项 XCTest（7 项跳过，包含需专用子进程驱动的强杀测试入口；该入口已由独立脚本实际执行）与 11 项 Swift Testing 零失败。正式签名包的实际界面验收通过，已安装启动 `/Applications/NDM.app` 构建 2026090810；安装后的 app.asar 与 NDMHost 均匹配最终包 SHA-256，启动与引擎健康检查后已永久清理本次旧包。

## 构建 2026090809

- Relay 1.4.6：具体视频页无候选时也能主动“解析当前页”；同一页面只显示一个主要视频入口，浅深主题采用中性色。
- 交接：校验当前导航和 top-frame 回执，等待中避免重复；离线、失去页面连接和超时可重试，popup 保持打开，反馈不冒充 native 下载成功。
- 媒体分类：unknown 或分类异常不再绕过视频解析，避免误存网页；已确认二进制文件与普通压缩包仍直接下载。

282 项脚本/界面逻辑测试、102 项 Relay 检查、14 项隔离 Chrome fixture、类型检查与构建通过。真实 Electron 验证 unknown/reject 分类下会解析并阻止普通下载，旧 build08 同一用例失败。浅深 popup 截图已检查。正式签名包实际界面同一验收通过，已安装启动 `/Applications/NDM.app` 构建 2026090809，安装后的 app.asar 和 NDMHost 均匹配最终包 SHA-256；启动与引擎健康后永久清理本次旧包。用户 Chrome 迁移仍需解锁，真实网站兼容不由模拟浏览器测试证明。

## 构建 2026090808

- 下载认证：按实际 HEAD/GET、编码路径和查询参数逐请求生成 Digest 签名；源站和代理分别维护认证状态，避免复用 nonce 计数或被预设 Basic 覆盖。
- 兼容性：支持 MD5 与 SHA-256；补 HTTP 转发代理的独立源站认证，跨源重定向不复用原站凭据。TLS、NTLM 继续使用系统默认处理；手动 HTTPS Digest CONNECT 尚不支持。
- Relay 1.4.5：保留B站当前分P与 Vimeo 非公开链接的访问字段，继续清除追踪参数，避免交接错视频或丢失访问上下文。

19 项原生针对性检查、279 项脚本/界面逻辑测试、96 项 Relay 检查、4 项隔离浏览器测试和类型检查通过。旧安装包的独立真实 Host 验收复现认证失败；开发 Host 完成 MD5/SHA-256 四路下载，4 MiB 随机文件哈希一致。完整原生回归已通过：863 项 XCTest（6 项环境跳过）与 11 项 Swift Testing，零失败；正式签名包内 Host 的独立认证下载验收同样通过。已安装启动 `/Applications/NDM.app` 构建 2026090808，app.asar 与 NDMHost 均匹配最终包 SHA-256，健康检查后永久清理本次旧包。实际 Chrome 插件迁移仍待解锁，不据随包版本宣称浏览器已更新。

## 构建 2026090807

- 速度历史：稳定速度的真实快照也能记入曲线，仍保留任务列表去重；移除采样值的 EMA，不定时捏造观测值。
- 曲线动效：平滑移动时间视口与纵轴，按真实样本重画，解决新增采样时的跳变；首个样本只显示点，填充不再覆盖未知历史。
- 边界：保留窗口左侧前驱、时钟倒退或长断流时重置；全零数据正确显示 0 B/s。运行中开启减少动态效果会立即停止动画。

279 项测试、类型检查、构建和独立真实界面验收通过。修复后稳定速度形成约 6.09 单位的时间跨度，新采样产生 36 种中间路径，填充与真实观测起止一致。综合下载动效 QA 也已通过；测试改用真实采样，并修正 FPS 计数区间，测得约 119.5 FPS。正式签名包同一实际界面验收通过（28 种中间路径），截图 `/tmp/ndm-speed-history-final.png` 已检查。已安装启动 `/Applications/NDM.app` 构建 2026090807，app.asar 和 NDMHost 均匹配最终包 SHA-256；启动健康后永久清理本次旧包。

采样为界面接收时刻、最多每秒两次，不能代表逐瞬时峰值或网络带宽基准。

## 构建 2026090806

- 分段显示：各段独立平滑推进，其他分段更新不再让已完成段从 100% 倒退；不再用总动画比例统一缩放全部分段。
- 准确性：即使两次快照间没有动画帧，真实回滚仍立即反映；暂停、减少动态效果、切换任务和变更分段范围各自处理历史，绘制总面积不超真实进度。
- 保留 Hero 单一帧时钟，总进度条与背景继续共享总体进度。

验证：7 项针对性测试、266 项脚本/界面逻辑测试、类型检查及构建通过。实际旧正式包完成段降到 83.3%；修复后的实际界面 62 帧采样均保持完成段为 100%，并验证回滚、暂停、任务切换及减少动态效果。

综合旧脚本 `qa-transfer-motion` 在旧 build 05 和本批均未全绿：速度曲线只有两种路径、1 个 cubic 段，与脚本的 4 路径/39 段预期不符；旧包还出现一次行进度步幅阈值失败。当前速度图只绘制真实已有采样，固定 39 段断言不再对应模型，但新采样导致路径拓扑变化时的动效仍需单独核查。未放宽脚本或把此项称为通过；新分段修复有独立实际帧回归证据。正式签名包独立运行同一界面回归通过，66 帧中完成段最小填充为 1。已安装启动 `/Applications/NDM.app` 构建 2026090806，app.asar 与 NDMHost 均匹配最终包 SHA-256；健康检查后永久清理本次旧部署包。

## 构建 2026090805

- 限速：修复单个响应数据块超过一秒配额时永久等待的问题，分多个窗口消费共享配额；等待中及时响应取消，并用单调时钟计算窗口。
- 暂停：低限速等待时能排空下载，不再被大块回调卡住；暂停后无迟到写入，解除限速后继续下载的文件哈希一致。
- 并发边界：多个连接仍共享配额；动态调高、调低、关闭限速和等待中缩短父分段均有针对性验证。

旧正式 Host 的隔离测试复现 128 KiB 文件在 32 KiB/s 下无法完成；修复后 debug Host 约 3.07 秒完成，等待中的暂停约 58 毫秒返回，恢复文件哈希正确。正式签名包内 Host 同一验收约 3.05 秒完成、暂停约 52 毫秒，哈希一致。

检查：33 项针对性测试通过。完整原生检查执行 854 项 XCTest（6 项既有环境跳过）及 11 项 Swift Testing；其中一个既有安装器测试因 `hdiutil create` 未生成测试 DMG 失败，其余通过。该安装器测试在源码未变的情况下单独复跑通过；首轮创建失败原因未确认，不能将首轮描述为全绿。日志：`/tmp/ndm-limiter-full-native.log`、`/tmp/ndm-limiter-installer-recheck.log`。正式构建及签名验证通过。

已安装启动 `/Applications/NDM.app` 构建 2026090805；安装后的 app.asar 和 NDMHost 均与最终包 SHA-256 一致。启动健康后已永久清理本次旧部署包。未更改用户浏览器扩展加载目录。

边界：仍采用一秒配额窗口，整块回调消费完额度才写盘，因此低速进度可能分批更新；不宣称精确限制网卡瞬时接收速率。Digest 的既有问题已单独记录在 `docs/product/AUTHENTICATION_FOLLOWUP_2026-09-08.md`，本批未改其处理。

## 构建 2026090804

- 分段下载：空闲连接接手剩余尾段时，保留父分段当前 HTTP 请求，只为子分段发起新请求；写入按新边界截断，完成后关闭父请求未使用的响应。
- 故障恢复：分段计划先持久化，再发布新写入边界；保留原有 416 回退、暂停和续传机制。
- 认证边界：Basic 重试采用最新所属范围；Digest、NTLM 与 TLS 保留原系统挑战处理，不承诺控制其内部重试范围。
- 参考证据：核验原版 ARM64 接收和写入函数，执行原指令的四组边界算术测试。逆向产物留在参考目录，产品不依赖其代码或旧 Swift 仓库。

隔离验证已证明：首次尾段拆分时强制终止真实 Host，重启后复用已写父分段，最终随机 8 MiB 文件 SHA-256 一致；debug 与正式签名包内 Host 均通过。848 项 XCTest（6 项既有环境跳过）、11 项 Swift Testing、259 项前端/脚本测试、类型检查及正式构建通过。仍使用独立分段加最终合并，未解决近两倍磁盘峰值。

已安装启动 `/Applications/NDM.app` 构建 2026090804；安装后的 app.asar 和已签名 NDMHost 均与最终包哈希一致，健康检查后永久清理本次旧部署包。浏览器 Relay 迁移仍待解锁，未在本批执行。独立审查另发现既有低限速下大数据块可能永久等待，以及手工 Digest 的 query/proxy 边界问题，列入后续修复，不宣称所有已知引擎问题已解决。

## 构建 2026090803

- Relay 1.4.4：由正在执行的后台 worker 报告版本，不读取刚替换的 manifest 冒充已更新；握手不等待回复、不阻断旧版 host 的下载。
- 设置：区分连接成功、版本未确认、需要更新和多个 worker 混用旧版；普通探测连接不会被误报为旧扩展。
- 桥接：统计主端口与备用端口，断线清除版本记录，同步读取连接数与身份，拒绝把无效握手当作有效版本。
- 边界：没有自动重载浏览器、刷新网页或完成加载目录迁移；已注入网页的脚本版本尚未由这项握手确认。

验证：841 项 XCTest（6 项既有环境跳过）与 11 项 Swift Testing 零失败；最终连接快照调整后的 16 项桥接测试与真实独立 host 握手验收通过。259 项前端/脚本测试、33 项隔离界面验收、94 项 Relay 检查、类型检查及构建通过。

已安装启动 `/Applications/NDM.app` 构建 2026090803。签名通过，已安装 app.asar 与已签名 NDMHost 的 SHA-256 均匹配最终构建包；启动与引擎健康验证后永久清理本次旧包。实际 Host 报告桥接可用、目标 Relay 1.4.4，本次检查时无浏览器连接。Chrome 迁移仍待手动解锁，未声称浏览器加载了新版。

## 构建 2026090802

- 完整性：用强 ETag 或符合条件的 Last-Modified 校验分段与续传，防止同尺寸源文件变化后混入旧数据；每次 Range 在写入前校验响应身份。
- 文件保护：合并先写独立临时文件，再独占发布；目标文件已存在时保留原文件，合并失败保留分段以便重试。
- 恢复与空间：记录合并临时文件的所有权，崩溃后的重试、删除及重新开始会精确回收；目标盘离线时保留清理凭据。合并中的暂停、取消可在分块边界生效。
- Relay 1.4.3：浮动入口随播放器显隐、移除与页面切换正确更新；键盘 Escape 返回焦点；交接失败可见且可重试；B 站离开视频页后不再提交旧视频。
- 连接状态：设置页显示实际桥接状态，浏览器正常断开后不再残留虚假的连接计数。
- 任务详情：切换任务后更新链接、删除确认和异步结果均属于各自任务，避免将 A 的链接提交给 B，或由旧删除回包关闭当前详情。
- 性能边界：无强验证器的服务器使用单次完整响应，可能降低吞吐，且不能承诺跨响应续传。普通分段合并仍可能接近两倍磁盘占用；这次没有宣称完成单文件偏移存储。

验证：840 项 XCTest（6 项既有环境跳过）及 11 项 Swift Testing，零失败；255 项前端/脚本测试、类型检查、构建通过。Relay 90 项检查及 8 项隔离浏览器测试通过。详情切换的 4 项真实界面回归通过，打包应用的连接调整、限速、恢复完成和清理验收通过。

已安装并启动 `/Applications/NDM.app`，版本为 2026.9.8 / 2026090802，正式签名通过。安装后的 app.asar 和已签名 NDMHost 均与最终构建包 SHA-256 一致；仅在启动与引擎验证成功后永久清理本次旧部署包。

浏览器待办：随包 Relay 为 1.4.3，但本次检查 Chrome 仍从旧 Swift 仓库目录加载 1.4.2。尝试迁移时电脑已锁屏，桌面工具要求手动解锁，故未完成迁移；不能据随包版本宣称实际浏览器已更新。

## 构建 2026090801

- 下载：自动接手未完成尾段时保留其他请求，服务器拒绝额外请求时退避排队；区分配置上限与活跃请求数。
- 进度：同时显示总进度与实际分段，修复开启动效时已完成分段显示不满的问题。
- 体验：响应式列宽和详情占位、复制反馈、静音取消操作、DMG 默认打开与显式安装。
- 更新：稳定签名保留文件夹授权；去掉覆盖版本号的旧 macOS 配置，包内版本和构建号必须与项目元数据一致。
- 空间：仅在新应用启动且引擎响应后永久清理本次部署的旧包；失败时保留回退包。该策略已获用户明确授权，不清空废纸篓。

验证：255 项前端/脚本测试通过，Release 构建与签名通过，已安装包 Info.plist、归档元数据和 UI preload 均为 2026.9.8 / 2026090801。相同构建号重复安装被拒绝。引擎与工作区行为验证见上一轮对应报告。
