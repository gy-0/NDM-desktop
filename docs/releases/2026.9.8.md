# NDM 2026.9.8

本机构建：2026090801。未作为公开商店版本发布。

- 下载：自动接手未完成尾段时保留其他请求，服务器拒绝额外请求时退避排队；区分配置上限与活跃请求数。
- 进度：同时显示总进度与实际分段，修复开启动效时已完成分段显示不满的问题。
- 体验：响应式列宽和详情占位、复制反馈、静音取消操作、DMG 默认打开与显式安装。
- 更新：稳定签名保留文件夹授权；去掉覆盖版本号的旧 macOS 配置，包内版本和构建号必须与项目元数据一致。
- 空间：仅在新应用启动且引擎响应后永久清理本次部署的旧包；失败时保留回退包。该策略已获用户明确授权，不清空废纸篓。

验证：255 项前端/脚本测试通过，Release 构建与签名通过，已安装包 Info.plist、归档元数据和 UI preload 均为 2026.9.8 / 2026090801。相同构建号重复安装被拒绝。引擎与工作区行为验证见上一轮对应报告。
